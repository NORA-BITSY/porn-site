<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>Ultimate Porn Space - {{pornstar_d}}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap">
        {{> _generalcss}}
    </head>
    <body class="bg-gradient">
        <div class="row">
            <div class="col-md-12">
                {{> _navbar}}
            </div>
        </div>
        <br>
        <div class="container py-5">
            {{> _top}}
            <br>
            <h1 class="display-4 fw-bold">{{pornstar_d}}</h1>
            <hr class="my-4">
            <div class="row g-4">
                {{#each results}}
                <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                    <div class="card border-0 shadow-lg h-100 overflow-hidden rounded-3 transition-all">
                        <img src="{{video.thumb}}" class="card-img-top img-fluid" loading="lazy" alt="{{video.title}}">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title fw-semibold text-truncate">{{video.title}}</h5>
                            <p class="card-text text-muted small">Views: {{video.views}} | Duration: {{video.duration}} | Rating: {{video.rating}}</p>
                            <div class="mt-auto d-flex gap-2">
                                <a href="/video/{{video.video_id}}" class="btn btn-primary flex-grow-1">Watch</a>
                                {{> _savebutton}}
                            </div>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>    
            <div class="row justify-content-center mt-5">
                <div class="col-auto">
                    {{> _pagination}}
                </div>
            </div>
        </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    {{> _generalscripts}}
    </body>
</html>